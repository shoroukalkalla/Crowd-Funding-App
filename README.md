# Run Project

```bash
git clone https://github.com/MohamedShehata15/Crowd-Funding-App.git
cd Crowd-Funding-App
python -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt
touch .env
	DB_NAME = 'crowd_funding'
	DB_PASSWORD = 'password'
	DB_HOST = 'localhost'
	DB_USER = 'root'
```
